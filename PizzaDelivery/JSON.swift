// The full JSON spec can be found at http://www.json.org/
//
// Using `Any` is not 100% percent correct to represent a JSON object, but it's
// acceptable for the practical purposes of this app.
typealias JSONObject = [String: Any]

typealias JSONArray = [JSONObject]
